-- ============================================
--  Hyprland Main Config
-- ============================================

-- Load modules
require("modules.config")      -- Look & feel, hl.config()
require("modules.monitor")    -- Monitor setup
require("modules.env")        -- Environment variables
require("modules.input")      -- Input config
require("modules.autostart")  -- Autostart / hooks
require("modules.binds")      -- Keybindings
require("modules.windowrules") -- Window & workspace rules
